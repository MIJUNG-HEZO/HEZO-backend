# 커밋 컨벤션

이 저장소는 [Conventional Commits](https://www.conventionalcommits.org/) 형식을 따른다.
커밋 메시지는 변경 의도를 빠르게 파악할 수 있도록 명확하고 일관되게 작성한다.

## 기본 형식

```text
<type>(<scope>): <subject>

<body>

<footer>
```

- `type`: 변경 종류를 나타낸다.
- `scope`: 변경 범위를 나타낸다. 선택 사항이며, 가능하면 소문자 영어 또는 도메인명을 사용한다.
- `subject`: 커밋 제목이다. 반드시 한글로 작성한다.
- `body`: 변경 이유나 상세 내용을 적는다. 선택 사항이다.
- `footer`: 이슈 번호, Breaking Change 등을 적는다. 선택 사항이다.

## 커밋 타입

| 타입 | 설명 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 동작에 영향이 없는 스타일 변경 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `test` | 테스트 추가 또는 수정 |
| `chore` | 빌드, 설정, 패키지 등 기타 작업 |
| `build` | 빌드 시스템 또는 외부 의존성 변경 |
| `ci` | CI 설정 및 스크립트 변경 |
| `perf` | 성능 개선 |
| `revert` | 이전 커밋 되돌리기 |

## 팀 규칙

1. 커밋 메시지는 한글로 작성한다.
   - 단, `type`, `scope`, `BREAKING CHANGE`, 이슈 키 등 관례적으로 쓰는 키워드는 영어를 허용한다.
2. 하나의 커밋에는 하나의 작업 단위만 담는다.
   - 기능 추가와 리팩터링, 포맷팅, 문서 수정은 가능한 한 분리한다.
3. 커밋 제목은 50자 이내로 작성한다.
4. 커밋 제목 끝에는 마침표를 붙이지 않는다.
5. 커밋 제목은 변경 결과가 아니라 변경 의도가 드러나게 작성한다.
6. 커밋 본문은 필요한 경우에만 작성하되, `무엇을`보다 `왜` 변경했는지를 중심으로 적는다.
7. 이슈와 관련된 커밋은 footer에 이슈 번호를 연결한다.
8. 미완성 작업은 `WIP` 커밋으로 남기지 않고, 가능하면 작업 단위가 완성된 뒤 커밋한다.
9. 민감 정보, 개인 환경 설정, 로컬 로그 파일은 커밋하지 않는다.
10. 테스트가 필요한 변경은 커밋 전에 관련 테스트를 실행하고 결과를 확인한다.

## 작성 예시

```text
feat(auth): 소셜 로그인 기능 추가
```

```text
fix(user): 회원 조회 시 탈퇴 회원이 포함되는 문제 수정
```

```text
docs: 커밋 컨벤션 문서 추가
```

```text
refactor(order): 주문 생성 로직을 서비스 계층으로 분리
```

```text
chore: 환경변수 예시 파일 추가
```

본문이 필요한 경우:

```text
fix(payment): 결제 승인 실패 시 재시도 조건 수정

PG사 응답 코드가 일시 오류인 경우에만 재시도하도록 조건을 제한한다.
중복 결제 가능성을 줄이기 위해 영구 실패 코드는 즉시 실패 처리한다.

Refs: #24
```

Breaking Change가 있는 경우:

```text
feat(auth)!: 토큰 재발급 API 응답 형식 변경

기존 `refreshToken` 필드를 제거하고 쿠키 기반 재발급 방식으로 변경한다.

BREAKING CHANGE: 클라이언트는 더 이상 응답 바디에서 refreshToken을 읽을 수 없다.
```

## 권장 커밋 흐름

1. 변경 전 작업 범위를 작게 정한다.
2. 변경 파일을 확인한다.

```bash
git status
git diff
```

3. 관련 파일만 스테이징한다.

```bash
git add <file>
```

4. 컨벤션에 맞게 커밋한다.

```bash
git commit -m "feat(auth): 소셜 로그인 기능 추가"
```

5. 커밋 후 변경 이력을 확인한다.

```bash
git log --oneline -5
```

## 다음에 정할 문서 후보

프로젝트 협업 규칙을 이어서 정리할 때 아래 문서를 추가하는 것을 권장한다.

- `BRANCH_STRATEGY.md`: 브랜치 종류, 브랜치 네이밍, 병합 전략
- `PR_CONVENTION.md`: PR 제목, 본문 템플릿, 리뷰 규칙, 머지 조건
- `ENVIRONMENT.md`: 환경변수 관리 방식, `.env` 파일 규칙, 비밀값 관리
- `NAMING_CONVENTION.md`: 패키지, 클래스, 함수, 변수, DB 테이블 네이밍 규칙
- `CODE_STYLE.md`: 포맷터, 린터, import 정렬, 예외 처리 규칙
