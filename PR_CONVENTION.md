# PR 컨벤션

이 저장소의 Pull Request는 변경 사항의 목적, 영향 범위, 검증 결과를 명확히 공유하기 위해 일관된 형식으로 작성한다.
PR 본문 템플릿은 별도 템플릿을 기준으로 운영하며, 팀 템플릿이 준비되면 이 문서에 추가한다.

## 기본 원칙

1. 모든 변경 사항은 PR을 통해 병합한다.
2. 하나의 PR에는 하나의 작업 단위만 담는다.
3. PR 크기는 리뷰 가능한 범위로 유지한다.
4. PR 제목과 설명은 한글로 작성한다.
5. PR 제목은 커밋 컨벤션의 타입을 따른다.
6. PR 본문에는 변경 목적, 주요 변경 사항, 검증 방법을 포함한다.
7. 리뷰어가 이해하기 어려운 맥락은 본문에 함께 남긴다.
8. UI 또는 API 응답 변경처럼 확인이 필요한 변경은 스크린샷, 요청/응답 예시, 참고 자료를 첨부한다.
9. 미완성 PR은 Draft PR로 생성한다.
10. 병합 전 충돌, 실패한 테스트, 미완료 TODO가 없는지 확인한다.

## PR 제목 규칙

```text
<type>(<scope>): <title>
```

- `type`: 변경 종류를 나타낸다.
- `scope`: 변경 범위를 나타낸다. 선택 사항이다.
- `title`: PR 제목이다. 한글로 작성한다.

PR 제목의 `type`은 커밋 컨벤션의 타입을 따른다.

| 타입 | 설명 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 동작에 영향이 없는 스타일 변경 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `test` | 테스트 추가 또는 수정 |
| `chore` | 빌드, 설정, 패키지 등 기타 작업 |
| `build` | 빌드 시스템 또는 외부 의존성 변경 |
| `ci` | CI 설정 및 스크립트 변경 |
| `perf` | 성능 개선 |
| `revert` | 이전 변경 사항 되돌리기 |

## PR 제목 예시

```text
feat(auth): 소셜 로그인 기능 추가
```

```text
fix(user): 탈퇴 회원 조회 조건 수정
```

```text
docs: 브랜치 전략 문서 추가
```

```text
refactor(order): 주문 생성 로직 분리
```

## PR 본문 템플릿

아래 템플릿을 기준으로 PR 본문을 작성한다.
선택 항목은 해당 사항이 없으면 `없음` 또는 `해당 없음`으로 표시한다.

```markdown
<!--
이 템플릿은 PR 작성을 돕기 위한 가이드입니다.
주석 안의 내용은 PR에 표시되지 않습니다.
-->

## 연결된 이슈
<!--
이 PR로 해결되는 이슈를 작성해주세요.
자동 종료가 필요한 경우 close / resolves 키워드를 사용하세요.
예: close #123
-->
-

---

## 구현 목표
<!--
이번 PR의 목적을 간단히 설명해주세요.
무엇을 왜 했는지 한 줄로 요약합니다.
-->
-

---

## 구현 내용
<!--
주요 변경 사항을 구체적으로 작성해주세요.
- 추가/수정된 기능
- 변경된 로직
- 영향 범위
-->
-

---

## 기술적 의사결정 (선택)
<!--
아래와 같은 내용이 있다면 작성해주세요.
- 구조/아키텍처 선택 이유
- 기존 방식에서 변경한 이유
- 사용한 기술/라이브러리 선택 근거
-->
-

---

## API 변경 사항 (선택)
<!--
API 요청/응답, 상태 코드, 에러 응답 변경이 있다면 작성해주세요.
-->
- 요청:
- 응답:
- 상태 코드:
- 에러 응답:

---

## 환경변수 / 설정 변경 (선택)
<!--
.env, Docker, DB, 외부 API 설정 변경이 있다면 작성해주세요.
민감한 값은 직접 작성하지 않습니다.
-->
- [ ] 환경변수 또는 설정 변경 없음
- [ ] 환경변수 또는 설정 변경 있음
  - 변경 내용:

---

## 보안 고려사항 (선택)
<!--
보안과 관련된 고려 사항이 있다면 작성해주세요.
- 인증/인가 처리
- 민감 정보 관리
- 권한 제어
-->
-

---

## 테스트 방법
<!--
테스트 방법 및 검증 내용을 작성해주세요.
-->
- [ ] 주요 기능 정상 동작 확인
- [ ] 에러 케이스 처리 확인
- [ ] 로컬 환경에서 실행 확인
- [ ] 추가 테스트 필요 (있다면 명시)

---

## 테스트 결과
<!--
실행한 명령어와 결과를 작성해주세요.
테스트를 실행하지 못한 경우 사유를 작성합니다.
-->

```bash
# 예: ./gradlew test
```

- 결과:

---

## 주요 변경 범위 (선택)
<!--
주요 변경 파일, 디렉토리, 모듈 또는 도메인을 작성해주세요.
예:
- auth 모듈
- user 조회 로직
- compose/docker-compose.yml
-->
-

---

## 스크린샷 / API 응답 예시 (선택)
<!--
UI 변경, API 응답 변경, 확인이 필요한 결과가 있다면 첨부해주세요.
-->
-

---

## 리뷰 요청 사항 (선택)
<!--
리뷰어가 중점적으로 봐줬으면 하는 부분이 있다면 작성해주세요.
-->
-

---

## 체크리스트
<!--
PR 제출 전 확인해주세요.
-->
- [ ] PR 제목 규칙을 지켰다.
- [ ] 하나의 PR에 하나의 작업 단위만 담았다.
- [ ] 변경 사항을 충분히 설명했다.
- [ ] 불필요한 로그/주석을 제거했다.
- [ ] 관련 이슈를 연결했다.
- [ ] 관련 테스트를 실행하고 결과를 작성했다.
- [ ] 문서 수정이 필요한 경우 반영했다.
- [ ] 환경변수 또는 설정 변경이 있는 경우 공유했다.
- [ ] API 변경이 있는 경우 요청/응답 예시를 첨부했다.
- [ ] 리뷰어가 집중해서 볼 부분을 작성했다.
```

## 작성 가이드

### 작업 개요

PR에서 해결하려는 문제나 추가하려는 기능을 짧게 설명한다.

좋은 예시:

```text
카카오 OAuth 인증을 통해 사용자가 소셜 로그인을 할 수 있도록 인증 흐름을 추가한다.
```

피해야 할 예시:

```text
로그인 작업
```

### 변경 사항

변경된 내용을 리뷰어가 빠르게 파악할 수 있도록 항목별로 정리한다.

```text
- 카카오 OAuth 인가 코드 검증 로직 추가
- 신규 사용자 생성 로직 추가
- 로그인 성공 시 JWT 발급 처리 추가
```

### 테스트 및 검증 방법

실행한 테스트와 수동 검증 내용을 작성한다.

```text
- ./gradlew test
- 카카오 로그인 성공 케이스 수동 확인
- 잘못된 인가 코드 요청 시 401 응답 확인
```

테스트를 실행하지 못한 경우에는 이유를 명확히 남긴다.

```text
- 테스트 미실행: OAuth 클라이언트 키가 로컬에 없어 수동 검증 불가
```

## 리뷰 규칙

1. 리뷰어는 동작 오류, 요구사항 누락, 보안, 성능, 유지보수성을 우선 확인한다.
2. 단순 취향 차이보다 팀 컨벤션과 실제 위험을 기준으로 리뷰한다.
3. 수정이 필요한 의견은 이유와 기대 방향을 함께 작성한다.
4. 질문, 제안, 필수 수정 요청을 구분해서 남긴다.
5. 작성자는 리뷰 의견에 반영 여부를 답변한다.
6. 반영하지 않는 의견은 이유를 남긴다.
7. 중요한 변경 요청은 코멘트 해결 전에 재확인을 받는다.

## 리뷰 코멘트 권장 표현

| 구분 | 의미 |
| --- | --- |
| `must` | 병합 전 반드시 수정해야 하는 사항 |
| `suggest` | 개선 제안 |
| `question` | 확인이 필요한 질문 |
| `note` | 참고 의견 |
| `nit` | 사소한 스타일 또는 취향 수준의 의견 |

예시:

```text
must: 인증 실패 시 500이 아니라 401을 반환해야 합니다.
```

```text
suggest: 중복 검증 로직을 별도 메서드로 분리하면 테스트하기 쉬울 것 같습니다.
```

```text
question: 이 API는 탈퇴 회원도 조회 대상에 포함해야 하나요?
```

## 머지 조건

1. PR 작성자가 셀프 리뷰를 완료한다.
2. 최소 1명 이상의 리뷰 승인을 받는다.
3. 필수 CI 또는 테스트가 통과한다.
4. 충돌이 없는 상태여야 한다.
5. PR 본문 체크리스트가 완료되어야 한다.
6. `must` 코멘트가 모두 해결되어야 한다.
7. 배포 영향이 있는 변경은 팀에 공유한 뒤 병합한다.

## 머지 방식

기본 머지 방식은 Squash Merge를 권장한다.

- PR 단위로 변경 이력을 관리한다.
- Squash Merge 메시지는 커밋 컨벤션을 따른다.
- 여러 작업이 섞인 PR은 병합 전에 작업 범위를 다시 확인한다.

커밋 이력을 보존해야 하는 경우에는 팀 합의 후 Merge Commit을 사용할 수 있다.

## Draft PR 규칙

Draft PR은 작업 공유, 중간 리뷰, 방향성 확인이 필요한 경우 사용한다.

Draft PR 상태에서는 다음 내용을 명확히 표시한다.

- 아직 완료되지 않은 작업
- 리뷰가 필요한 부분
- 막힌 부분 또는 의사결정이 필요한 부분
- 테스트 미완료 항목

작업이 완료되면 Ready for review 상태로 변경한다.

## PR 체크리스트 권장 항목

팀 템플릿을 추가할 때 아래 체크리스트를 포함하는 것을 권장한다.

```markdown
- [ ] 작업 범위가 하나의 목적에 맞게 정리되어 있다.
- [ ] 커밋 메시지 또는 Squash Merge 메시지가 컨벤션을 따른다.
- [ ] 관련 테스트를 실행했다.
- [ ] 문서 수정이 필요한 경우 반영했다.
- [ ] 환경변수 변경이 있는 경우 공유했다.
- [ ] API 변경이 있는 경우 요청/응답 예시를 첨부했다.
- [ ] 리뷰어가 집중해서 볼 부분을 작성했다.
```

## 다음에 정할 문서 후보

PR 컨벤션과 함께 아래 문서를 추가하는 것을 권장한다.

- `ENVIRONMENT.md`: 환경변수 관리 방식, `.env` 파일 규칙, 비밀값 관리
- `NAMING_CONVENTION.md`: 패키지, 클래스, 함수, 변수, DB 테이블 네이밍 규칙
- `CODE_STYLE.md`: 포맷터, 린터, import 정렬, 예외 처리 규칙
