# 코드 스타일 규칙

이 문서는 프로젝트의 코드 작성 스타일과 품질 기준을 정의한다.
코드 스타일은 개인 취향보다 일관성, 가독성, 유지보수성을 우선한다.

## 기본 원칙

1. 코드 스타일은 팀에서 합의한 포맷터와 린터를 기준으로 맞춘다.
2. 포맷팅만 변경하는 작업은 기능 변경과 분리한다.
3. 읽기 쉬운 코드를 위해 의도를 드러내는 이름과 작은 단위를 유지한다.
4. 중복 제거보다 명확성이 더 중요한 경우 무리하게 추상화하지 않는다.
5. 하나의 함수나 메서드는 하나의 책임을 갖도록 작성한다.
6. 예외, 로그, 테스트는 기능 구현의 일부로 함께 작성한다.
7. 사용하지 않는 코드, 주석, 로그, import는 남기지 않는다.
8. 외부 API, 라이브러리, 프레임워크가 요구하는 스타일은 해당 규칙을 따른다.
9. 코드 스타일 변경 범위가 큰 경우 PR에서 변경 의도와 영향 범위를 설명한다.
10. 자동화 가능한 규칙은 수동 리뷰보다 도구로 검증한다.

## 포맷터 / 린터

프로젝트는 포맷터와 린터를 사용해 코드 스타일을 자동으로 맞춘다.
실제 기술 스택이 확정되면 사용하는 도구와 실행 명령어를 이 섹션에 추가한다.

권장 운영 방식:

1. 포맷터는 코드 모양을 자동으로 통일한다.
2. 린터는 잠재 오류, 안티패턴, 사용하지 않는 코드를 검출한다.
3. CI에서 포맷터 또는 린터 검사를 실행한다.
4. PR 병합 전 포맷터와 린터 오류가 없어야 한다.
5. 팀원마다 다른 IDE 설정으로 스타일이 흔들리지 않도록 설정 파일을 저장소에 포함한다.

예시:

```bash
# formatter
./gradlew spotlessApply

# linter / check
./gradlew check
```

도구가 정해지기 전에는 사용하는 프레임워크의 기본 스타일을 따른다.

## 들여쓰기와 줄 길이

1. 들여쓰기는 사용하는 언어와 포맷터 기본값을 따른다.
2. 탭과 스페이스를 섞지 않는다.
3. 한 줄이 지나치게 길어지면 의미 단위로 줄바꿈한다.
4. 메서드 체이닝은 읽기 쉬운 단위로 줄바꿈한다.
5. 조건문이 길어지면 의미 있는 변수나 별도 메서드로 분리한다.

예시:

```text
if (user.isActive() && user.hasPermission() && !user.isDeleted()) {
    ...
}
```

조건이 길다면 아래처럼 의도를 드러낸다.

```text
boolean canAccess = user.isActive() && user.hasPermission() && !user.isDeleted();

if (canAccess) {
    ...
}
```

## import 규칙

1. 사용하지 않는 import는 제거한다.
2. wildcard import는 사용하지 않는다.
3. import 정렬은 IDE 또는 포맷터 설정을 따른다.
4. 정적 import는 테스트 가독성을 높이는 경우에만 사용한다.
5. 같은 패키지 내 import 순서로 인한 불필요한 diff가 생기지 않도록 자동 정렬한다.

권장 순서:

1. 표준 라이브러리
2. 외부 라이브러리
3. 프로젝트 내부 모듈
4. 정적 import

## 함수 / 메서드 작성

1. 함수는 한 가지 책임만 갖도록 작성한다.
2. 함수 이름은 동작과 의도를 명확히 드러낸다.
3. 매개변수가 많아지면 객체로 묶는 것을 고려한다.
4. 반환 타입과 예외 가능성을 호출자가 예측할 수 있게 작성한다.
5. 깊은 중첩은 조기 반환 또는 메서드 분리로 줄인다.
6. 사이드 이펙트가 있는 함수는 이름에서 의도가 드러나야 한다.

권장:

```text
createUser
validatePassword
issueAccessToken
```

피하기:

```text
process
handle
doWork
```

## 조건문

1. 복잡한 조건식은 의미 있는 변수나 메서드로 분리한다.
2. 부정 조건이 중첩되지 않도록 한다.
3. 예외 상황은 먼저 처리하고 정상 흐름은 아래에 둔다.
4. `else`가 불필요한 경우 조기 반환을 사용한다.

권장:

```text
if (user == null) {
    throw new UserNotFoundException();
}

return user;
```

피하기:

```text
if (user != null) {
    return user;
} else {
    throw new UserNotFoundException();
}
```

## 예외 처리

1. 예외는 숨기지 않는다.
2. 예외 메시지는 문제 원인과 필요한 맥락을 포함한다.
3. 민감 정보는 예외 메시지에 포함하지 않는다.
4. 복구 가능한 예외와 복구 불가능한 예외를 구분한다.
5. 단순히 로그만 남기고 예외를 삼키지 않는다.
6. 도메인에서 의미 있는 실패는 도메인 예외로 표현한다.
7. 외부 API 호출 실패는 호출 대상, 실패 원인, 재시도 가능 여부를 구분한다.

피하기:

```text
try {
    ...
} catch (Exception e) {
    log.error("error");
}
```

권장:

```text
try {
    ...
} catch (ExternalApiException e) {
    log.error("카카오 사용자 정보 조회 실패: status={}", e.getStatusCode(), e);
    throw new OAuthLoginFailedException();
}
```

## 로그

로그는 문제를 추적하기 위한 정보만 남긴다.
민감 정보와 과도한 로그는 남기지 않는다.

로그 레벨 기준:

| 레벨 | 사용 기준 |
| --- | --- |
| `ERROR` | 요청 실패, 시스템 오류, 즉시 확인이 필요한 문제 |
| `WARN` | 비정상 흐름이지만 서비스는 계속 가능한 문제 |
| `INFO` | 주요 비즈니스 이벤트 또는 상태 변화 |
| `DEBUG` | 개발 중 상세 확인이 필요한 정보 |
| `TRACE` | 매우 상세한 흐름 추적 |

규칙:

1. 비밀번호, 토큰, 인증 코드, Secret 값은 로그에 남기지 않는다.
2. 사용자 식별자는 필요한 경우에만 남긴다.
3. 반복 호출되는 로직에 과도한 `INFO` 로그를 남기지 않는다.
4. 예외 로그는 stack trace가 필요한지 판단해 남긴다.
5. 임시 디버깅 로그는 PR 전에 제거한다.

## 주석

주석은 코드가 설명하지 못하는 의도와 맥락을 보완할 때만 작성한다.

권장:

```text
// OAuth 제공자마다 사용자 식별자 필드명이 달라 provider별 매핑을 분리한다.
```

피하기:

```text
// user를 저장한다.
saveUser(user);
```

규칙:

1. 코드와 같은 내용을 반복하는 주석은 작성하지 않는다.
2. 임시 주석, 주석 처리된 코드, TODO는 PR 전에 정리한다.
3. TODO가 필요한 경우 이유와 후속 이슈를 함께 남긴다.
4. 복잡한 비즈니스 규칙은 주석보다 테스트와 문서로 보완한다.

TODO 예시:

```text
// TODO(#42): 정산 정책 확정 후 수수료 계산 기준을 반영한다.
```

## DTO / 요청 / 응답

1. 요청 DTO와 응답 DTO를 구분한다.
2. 외부에 노출되는 응답 필드는 명시적으로 관리한다.
3. 엔티티를 API 응답으로 직접 노출하지 않는다.
4. 검증 규칙은 가능한 DTO 또는 입력 경계에서 처리한다.
5. 응답 필드 추가, 삭제, 이름 변경은 PR에 명확히 작성한다.

권장:

```text
CreateUserRequest
UserResponse
UpdateProfileRequest
```

피하기:

```text
UserDto
DataResponse
ResultDto
```

## 도메인 로직

1. 핵심 비즈니스 규칙은 도메인 또는 서비스 계층에 명확히 둔다.
2. 컨트롤러에는 요청/응답 처리와 위임 로직만 둔다.
3. 데이터 접근 로직과 비즈니스 판단을 섞지 않는다.
4. 외부 API 연동 로직은 별도 클라이언트나 어댑터로 분리한다.
5. 트랜잭션 범위는 필요한 만큼만 유지한다.

## 테스트 코드

1. 기능 변경에는 가능한 한 테스트를 함께 작성한다.
2. 테스트 이름은 조건과 기대 결과를 드러내게 작성한다.
3. 테스트는 독립적으로 실행 가능해야 한다.
4. 테스트 간 실행 순서에 의존하지 않는다.
5. 외부 API, 시간, 랜덤 값은 제어 가능한 방식으로 다룬다.
6. 정상 케이스와 주요 예외 케이스를 함께 검증한다.
7. 테스트 데이터는 의미 있는 이름으로 작성한다.

권장 구조:

```text
given: 테스트 준비
when: 실행
then: 검증
```

예시:

```text
given 중복된 이메일이 존재하고
when 같은 이메일로 회원가입을 요청하면
then 회원가입에 실패한다
```

## 보안

1. 인증/인가 검사는 명시적으로 처리한다.
2. 사용자 입력값은 신뢰하지 않고 검증한다.
3. SQL, path, command, template 등에 사용자 입력을 직접 연결하지 않는다.
4. 민감 정보는 응답, 로그, 예외 메시지에 포함하지 않는다.
5. 권한이 필요한 API는 테스트에서 권한 실패 케이스도 검증한다.
6. 보안 관련 변경은 PR에서 별도 항목으로 공유한다.

## 의존성

1. 새 의존성을 추가하기 전에 표준 라이브러리나 기존 의존성으로 해결 가능한지 확인한다.
2. 의존성 추가 이유를 PR에 작성한다.
3. 사용하지 않는 의존성은 제거한다.
4. 버전 변경은 영향 범위와 마이그레이션 필요 여부를 확인한다.
5. 보안 취약점이 있는 의존성은 업데이트하거나 대체한다.

## 코드 리뷰 기준

리뷰에서는 아래 항목을 우선 확인한다.

1. 요구사항을 충족하는가
2. 예외 상황이 처리되어 있는가
3. 테스트가 필요한 부분에 테스트가 있는가
4. 보안상 위험한 부분이 없는가
5. 이름과 구조가 의도를 잘 드러내는가
6. 불필요한 중복이나 과도한 추상화가 없는가
7. 로그와 에러 메시지가 적절한가
8. API 변경 사항이 문서화되어 있는가

## PR 전 확인 사항

PR 생성 전 아래 항목을 확인한다.

```markdown
- [ ] 포맷터를 실행했다.
- [ ] 린터 또는 정적 검사를 통과했다.
- [ ] 관련 테스트를 실행했다.
- [ ] 사용하지 않는 import, 변수, 주석, 로그를 제거했다.
- [ ] 예외 처리와 로그가 적절한지 확인했다.
- [ ] 민감 정보가 코드, 로그, 문서에 포함되지 않았는지 확인했다.
- [ ] API 변경이 있다면 PR 본문에 작성했다.
- [ ] 환경변수 변경이 있다면 `.env.example`과 PR 본문에 반영했다.
```

## 스택 확정 후 추가할 항목

기술 스택이 확정되면 아래 내용을 추가한다.

- 사용 포맷터와 설정 파일
- 사용 린터와 실행 명령어
- IDE 설정 공유 방식
- 패키지 구조
- 테스트 실행 명령어
- CI 검사 항목
- 예외 응답 형식
- 로그 포맷

## 관련 문서

- `COMMIT_CONVENTION.md`: 커밋 메시지 규칙
- `BRANCH_STRATEGY.md`: 브랜치 운영 전략
- `PR_CONVENTION.md`: PR 작성 및 리뷰 규칙
- `ENVIRONMENT.md`: 환경변수 관리 규칙
- `NAMING_CONVENTION.md`: 이름 작성 규칙
